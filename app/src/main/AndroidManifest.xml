<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.darkchat">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DarkChat">
        <activity
            android:name=".Dashboard.RequestActivity"
            android:exported="true"
            android:label="@string/title_activity_request"
            android:theme="@style/Theme.DarkChat.NoActionBar" />
        <activity android:name=".Dashboard.SearchActivity" />
        <activity android:name=".Dashboard.SettingActivity" />
        <activity android:name=".Dashboard.ChatActiviy" />
        <activity android:name=".Dashboard.Dashboard" />
        <activity android:name=".Dashboard.userSingnUp" />
        <activity android:name=".Dashboard.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>